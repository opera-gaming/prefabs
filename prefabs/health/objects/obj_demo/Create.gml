// Two bars side by side: one with a window, one without. Holding the key
// empties the second almost instantly and barely touches the first, which
// is the entire reason the window exists.
guarded = health_make(10, 0.8);
raw = health_make(10, 0);
