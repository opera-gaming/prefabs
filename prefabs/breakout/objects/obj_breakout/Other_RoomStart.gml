// Counted here, not in Create: Create runs as each instance is made, so a
// controller placed first in the layer would count a room still half-built.
total_bricks = instance_number(obj_brick);
breakout_serve();
