::kernel::kernel_state_set(::kernel::kernel_states().title);

// Seconds, not frames: at 120Hz a frame counter runs the wiggle twice as
// fast, and this is the single most common way title screens differ
// between machines.
t = 0;
