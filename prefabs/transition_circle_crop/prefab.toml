# Prefab manifest — a derived transition. Inherits everything from
# transition-base and overrides only the fragment shader.
name = "Transition Circle Crop"
description = "A circle-crop room transition — the outgoing room collapses into a shrinking circle over a background colour before the incoming room fills back in."
category = "effects"
tags = ["effect:transition", "effect:circle-crop", "role:library"]

[parent-link]
# Sibling prefab in this catalog; short form resolves it as a local
# sibling directory instead of a network fetch.
source = "transition_base@v0.6.4"
