#:schema .gm-schema/v1.json#project

schema_version = 1
name = "top_down_arena"

# The order here is the flow: each controller sends you to the next room.
[room_order]
order = ["rm_title", "rm_arena", "rm_results"]
